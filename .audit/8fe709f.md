# audit(8fe709f) — empty ZenHive `enabledPlugins` hygiene pass

**Range audited (already landed on `main`):**
- `8fe709f` `chore: remove uninstalled ZenHive plugin references`

Covered span named by the dispatch: `ecfcb4c..8fe709f` (that exclusive range is this single commit). Integrated revision for QA: `8fe709f734d39ea29032b8e05b694e8bf8158d29`.

No prior audit marker in this repo.

## What was delivered

One-line operator-config cleanup: `.claude/settings.json` dropped the three enabled marketplace plugins (`elixir@zenhive`, `elixir-workflows@zenhive`, `harness@zenhive`) and left `"enabledPlugins": {}`. `extraKnownMarketplaces.zenhive` (GitHub `ZenHive/claude-marketplace`) stayed. No library, test, or CHANGELOG bytes moved.

## Hygiene review

- **Landed diff.** Correct for the stated chore. Empty `enabledPlugins` is a valid "none enabled" record; omitting the key would not change Claude Code behaviour. The remaining `extraKnownMarketplaces` entry is the marketplace catalog, not an enabled plugin — considered leftover, judged keep: Claude Code can still resolve ZenHive plugins without auto-enabling them.
- **Stale docs (fixed).** `AGENTS.md` had drifted from `CLAUDE.md` via transitive `~/.claude/includes/harness-workflow.md` (new "Integrated audit and QA" section, implementer question channel, `qa_command` wording, question-held lifetime notes). The landed commit did not touch `CLAUDE.md`; the drift is include-side. Regenerated with the marketplace copy of `sync-agents-md.sh` (never hand-edited). `--check` is green after regen.
- **CHANGELOG.** No gap. `.claude/settings.json` is not in the hex package `files:` list and is not consumer API.
- **Dead code / debug / naming.** None in the range. Repo-wide `TODO`/`FIXME`/`IO.inspect` scan on `*.ex`/`*.exs` is clean (Credo TagTODO mentions in `mix.exs` / `.credo.exs` only).
- **Reviewer rejections.** None recorded for this project. No false-rejection note.
- **Roadmap.** This repo's `roadmap/tasks.toml` had schema + phase 1 only (no `[[task]]`, no `ROADMAP.md`). Filing the QA repair required declaring `[bundles.qa-gates]`, a `ROADMAP.md` skeleton with phase-1 markers, and `[focus] phase = 1`. `rmap new --from-stdin` cannot append the first task when no `[[task]]` array exists (`MutateError::MissingTasks`); a throwaway seed row was used so the CLI could allocate id 2, then removed before render.

## Full-project QA at `8fe709f`

Command: `mix precommit.full` (cold tree: `mix deps.get` first). Log: `/tmp/harness-audit-precommit-full.jUDd95.log`. Alias exit 0.

| Step | Outcome |
|---|---|
| `format --check-formatted` | passed (alias continued) |
| `compile --warnings-as-errors` | passed for this app; yamerl/doctor/dialyzer_json compile warnings are in deps |
| `credo --strict --ignore TagTODO,TagFIXME` | 47 files, no issues |
| `doctor --raise` | 24 modules, 100% doc/moduledoc/spec coverage |
| `ex_dna --max-clones 0` | no duplication (23 files) |
| `reach.check --arch --smells` | architecture OK; smells none (advisory) |
| `sobelow --skip --exit low` | SCAN COMPLETE; only the expected non-Phoenix "cannot find the router" warning |
| `mix test.json --cover --cover-threshold 85 --exclude integration` | 333 passed, 7 excluded, 0 failed, 340 total; coverage 91.07% (threshold 85 met) |
| `dialyzer` | Total errors: 0 (cold PLT built) |
| `agents.check` | **skipped** inside the alias: `/home/harness/_DATA/code/claude-marketplace/scripts/sync-agents-md.sh` absent |
| `mix deps.audit` (extra, not in alias) | No vulnerabilities found |

Independent `sync-agents-md.sh --check` from `~/.claude/plugins/marketplaces/zenhive/scripts/` (and the grok marketplace-cache copy) at the same revision: **STALE**. That is the determinate freshness result `mix precommit.full` never ran.

QA judgment for revision `8fe709f`: **failed**. The alias exited 0 only because `agents.check` skipped a missing operator-checkout path; the same script from an installed marketplace copy failed. Missing-prerequisite skip is not a pass; the independent run is the failed check.

Repair owned here: regenerated `AGENTS.md` (hygiene). Mix-level lookup of marketplace copies is a behaviour change — filed as task 2, not patched in this audit.

## Filed tasks

- **Task 2** — Run `AGENTS.md` freshness check from an installed marketplace copy when the operator checkout is absent (`assignee = "cursor"`, `model = "cursor-grok-4.6-high"`).

## Outcome

The landed chore is a correct one-file settings cleanup. Hygiene found stale `AGENTS.md` (transitive include drift hidden by the skipped freshness gate) and regenerated it. Full `ci` steps at `8fe709f` were green; `agents.check` was not.

**Commit:** `audit(8fe709f): regen stale AGENTS.md; file agents.check marketplace lookup`

(The `.harness/audit.json` machine summary is written alongside but not committed, per the audit contract.)
